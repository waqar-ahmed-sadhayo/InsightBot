# Minimal deps for the deployed web app (Flask API + frontend) only --
# the full project requirements.txt also covers ingestion/scraping
# (scrapy, pymongo, etc.) which the live web app never imports.
Flask>=3.0
Flask-SQLAlchemy>=3.1
Flask-JWT-Extended>=4.6
Flask-Cors>=4.0
python-dotenv>=1.0
SQLAlchemy>=2.0
PyYAML>=6.0
